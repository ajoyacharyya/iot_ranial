package com.ranial.webservice.impl;

import org.osgi.framework.ServiceReference;


public class AppTest /*extends OSGiTestCase*/ {

   /* public void testHelloAvailability() {
    	System.out.println(context);
        ServiceReference ref = context.getServiceReference(Mediator.class.getName());
        System.out.println(ref);
       assertNotNull("Assert Availability", ref);
    }*/

    /*public void testHelloMessage() {
        ServiceReference ref = 
             context.getServiceReference(TemperatureSensor.class.getName());
        assertNotNull("Assert Availability", ref);
        TemperatureSensor hs = (TemperatureSensor) context.getService(ref);
        String message = hs.getCurrentTemp("100");
        System.out.println(message);
        assertNotNull("Check the message existence", message);
        assertEquals("Check the message", "hello", message);
    }*/

}